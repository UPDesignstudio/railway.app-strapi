'use strict';

/**
 * powertrain-collection service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::powertrain-collection.powertrain-collection');
