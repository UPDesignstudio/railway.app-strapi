'use strict';

/**
 * powertrain-collection controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::powertrain-collection.powertrain-collection');
